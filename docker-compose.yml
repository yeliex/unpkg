version: "3"

networks:
  localnet: {}

volumes:
  npmvol: {}

services:

  cache:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    networks:
      - localnet

  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "5000:5000"
    volumes:
      - npmvol:/npm
    networks:
      - localnet
    env_file: .env
    depends_on:
      - cache

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    networks:
      - localnet
    env_file: .env
    depends_on:
      - cache
